export { default as Author } from './Author';
export { default as Event } from './Event';

// TODO(tim): These are not necessarily specific to models but as long as we do
// not use them elsewhere, defining these interfaces here is fine.

export interface ICollection<M> extends ReadonlyArray<M> {
  readonly size: number;
}
