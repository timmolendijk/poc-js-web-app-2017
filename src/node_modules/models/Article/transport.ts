import { IListParams, IListResult } from 'transport';
import { Article } from 'models';

export interface IArticleListParams extends IListParams {
  readonly query: string;
  readonly limit?: number;
}

export interface IArticleListResult extends IListResult<Article, IArticleListParams> {}
