import { setHiddenProp } from '.';

const id: PropertyDecorator = (target, name: string | symbol, descriptor?): void => {
  setHiddenProp(target.constructor, 'getId', object => object[name]);
}

export default id;
