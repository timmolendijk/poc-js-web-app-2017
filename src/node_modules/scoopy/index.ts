export const NS = '@@scoopy';

export type IIdentifier = string | number;

export interface IRef {
  // [NS]: 'ref';
  type: string;
  id: IIdentifier;
}

export type IState = {
  // [`${NS}/nextId`]: number;
  [type: string]: {
    [id: string]: {
      [field: string]: boolean | string | number | IRef;
    };
  };
}

export { resetNextId } from './store';
export { default as field } from './field';
export { getPending, pending } from './pending';
