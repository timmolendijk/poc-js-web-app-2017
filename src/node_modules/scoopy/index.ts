export const NS = '@@scoopy';

export type IIdentifier = string | number;

export interface IRef {
  // [NS]: 'ref';
  type: string;
  id: IIdentifier;
}

export type IState = {
  [type: string]: {
    [id: string]: {
      [field: string]: boolean | string | number | IRef;
    };
  };
}

export { getPending } from './pending';
